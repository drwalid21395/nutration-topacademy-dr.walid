/*
==================================================
شرح الملف للمبتدئ
==================================================

اسم الملف:
tailwind.config.ts

وظيفة الملف:
إعدادات مكتبة Tailwind CSS — نعرّف هنا "الألوان والخطوط
والظلال والحركات" الخاصة بالموقع لاستخدامها في كل مكان
عبر أسماء مثل: bg-ocean-600 أو text-gold-400.

لماذا نحتاجه؟
بدون هذا الملف سيكون لدينا ألوان Tailwind الافتراضية فقط
(أحمر، أزرق، أخضر...). هنا نضيف "هوية الموقع":
- درجات المحيط (ocean) = درجات زرقاء متدرجة (لون الماء).
- الذهبي (gold) = لون العلامة التجارية.
- خط Cairo = خط عربي جميل.

متى يعمل؟
مرة واحدة عند بناء الـ CSS (في dev أو build).
يقرأه Tailwind تلقائيًا — لا نستدعيه.

العلاقة مع الملفات:
أي ملف داخل src/ يستخدم أسماء مثل bg-ocean-600 يعتمد على هذا الملف.

ملاحظة تعليمية:
لو حذفت هذا الملف سيظهر الموقع بدون ألواننا المخصصة
(كل أسماء ocean/gold ستصبح بلا معنى). لا تغيّر قيم الألوان
إلا بطلب من صاحب المشروع لأنها هوية العلامة التجارية.
==================================================
*/

// import type:
// نستورد "النوع" Config فقط (ليس قيمة). هذا للتأكد من أن
// شكل الكائن صحيح، وليس له أي دور أثناء التشغيل.
import type { Config } from 'tailwindcss';

const config: Config = {
  // content:
  // نقول لـ Tailwind: افحص هذه الملفات وابحث عن أسماء الفئات
  // (مثل bg-ocean-600) حتى يولّد الـ CSS النهائي منها فقط.
  // أي فئة غير مستخدمة لا تدخل في الملف النهائي = موقع أسرع.
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],

  theme: {
    extend: {
      // colors:
      // هنا نضيف ألوانًا جديدة. كل لون له 10 درجات (50 = أفتح، 950 = أغمق).
      colors: {
        // ocean = أزرق المحيط (اللون الرئيسي للموقع).
        ocean: {
          50: '#eef7fc',
          100: '#d6ebf8',
          200: '#b0d9f0',
          300: '#7cc0e5',
          400: '#419fd5',
          500: '#1d84bc',
          600: '#15699d',
          700: '#155480',
          800: '#15476b',
          900: '#0e3552',
          950: '#0a2438',
        },
        // lagoon = أزرق فيروزي فاتح (لون الماء الصافي).
        lagoon: {
          400: '#22c7c7',
          500: '#17a8ab',
          600: '#12878c',
        },
        // gold = ذهبي (لون التمييز: شعار الطبيب وأزرار مهمة).
        gold: {
          300: '#f2cf6b',
          400: '#eab84b',
          500: '#d9a23a',
          600: '#b8862c',
        },
      },

      // fontFamily:
      // نحدد خطًا رئيسيًا اسمه sans.
      // النص العربي يظهر بخط Cairo (أول خيار)،
      // وإن لم يتوفر ينتقل للخط التالي في القائمة بالترتيب.
      fontFamily: {
        sans: [
          'Cairo',
          'Tahoma',
          'system-ui',
          '-apple-system',
          'Segoe UI',
          'sans-serif',
        ],
      },

      // boxShadow: ظلال للبطاقات حتى تظهر وكأنها "مرتفعة".
      // نستخدمها بأسماء مثل shadow-card.
      boxShadow: {
        card: '0 4px 20px -4px rgba(10, 36, 56, 0.12)',
        'card-lg': '0 12px 40px -8px rgba(10, 36, 56, 0.2)',
      },

      // backgroundImage: خلفيات جاهزة تُستخدم بأسماء مثل bg-hero-waves.
      // hero-waves = تدرج أزرق يوحي بأمواج البحر في بداية الصفحة الرئيسية.
      backgroundImage: {
        'hero-waves':
          'radial-gradient(ellipse 80% 50% at 50% -20%, rgba(125,192,229,0.25), transparent), linear-gradient(180deg, #0a2438 0%, #0e3552 55%, #155480 100%)',
        'water-pattern':
          "url('/images/water-lanes.svg')",
      },

      // keyframes:
      // نعرّف "حركات" (Animation) باستخدام خطوات البداية/النهاية.
      keyframes: {
        // fade-up: العنصر يظهر من الأسفل مع تلاشي.
        'fade-up': {
          '0%': { opacity: '0', transform: 'translateY(16px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        // float: العنصر يطفو لأعلى وأسفل باستمرار (مثل عنصر عائم).
        'float': {
          '0%, 100%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-8px)' },
        },
        // wave: حركة أمواج مستمرة أفقية.
        'wave': {
          '0%': { transform: 'translateX(0)' },
          '100%': { transform: 'translateX(-50%)' },
        },
      },

      // animation:
      // نعطي كل حركة اسمًا قصيرًا + مدتها وسلوكها.
      // نستخدمها بأسماء مثل animate-fade-up.
      animation: {
        'fade-up': 'fade-up 0.6s ease-out both',
        float: 'float 4s ease-in-out infinite',
        wave: 'wave 12s linear infinite',
      },
    },
  },

  // plugins:
  // إضافات خارجية (لا نستخدم أيًا منها حاليًا — المصفوفة فارغة).
  plugins: [],
};

// export default:
// نُصدّر الإعداد ليكون الإعداد الافتراضي لتطبيق Tailwind.
export default config;

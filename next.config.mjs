/*
==================================================
شرح الملف للمبتدئ
==================================================

اسم الملف:
next.config.mjs

وظيفة الملف:
هذا "ملف إعدادات Next.js". يحتوي على قواعد أخبر فيها
Next.js كيف يتصرف: تشغيل الصور، ترويسات الأمان، وغيرها.

لماذا نحتاجه؟
Next.js يعمل بإعدادات افتراضية، لكننا نريد تخصيصها:
1) منع أي جهة من وضع الموقع داخل إطار (clickjacking).
2) إضافة ترويسات أمان HTTP لكل الصفحات.
3) السماح بعرض الصور من أي موقع خارجي (روابط صور الوجبات).

متى يعمل؟
مرة واحدة عند بدء تشغيل الخادم (dev أو build)، وليس عند كل طلب مستخدم.

من يستدعي هذا الملف؟
Next.js نفسه يقرؤه تلقائيًا عند الإقلاع — لا نستدعيه من أي مكان.

ملاحظة تعليمية:
هذا الملف بصيغة ESM (الامتداد .mjs). النقطة المهمة هنا:
الأسطر داخل الكائن هي "خيارات"، وكل خيار يغيّر سلوك Next.js.
لو حذفت أي خيار سيعود السلوك الافتراضي فقط — لن يتعطل الموقع.
==================================================
*/

/** @type {import('next').NextConfig} */
// JSDoc: هذا السطر تعليق خاص يخبر محرر الأكواد بنوع الكائن
// القادم (NextConfig) حتى يوفر لك اقتراحات عند الكتابة.
// لا يغيّر سلوك البرنامج إطلاقًا.
const nextConfig = {
  // reactStrictMode:
  // يفعّل الوضع الصارم في React — يساعدنا على اكتشاف أخطاء
  // المكونات أثناء التطوير (مثل دوال مؤثرة تعمل مرتين).
  reactStrictMode: true,

  // poweredByHeader:
  // Next.js يرسل ترويسة تقول للمتصفح "أنا خادم Next".
  // نخفيها لأسباب أمنية (لا نريد أن يعرف المخترقون تقنيتنا).
  poweredByHeader: false,

  // serverExternalPackages:
  // مكتبة pdfmake تعمل على الخادم فقط (توليد ملفات PDF).
  // نخبر Next.js ألا يحاول دمجها في الحزم التي تصل للمتصفح
  // لأنها تحتاج مكتبات Node.js لا تعمل في المتصفح.
  serverExternalPackages: ['pdfmake'],

  images: {
    // remotePatterns:
    // سماح بعرض الصور من أي موقع خارجي (https).
    // مثال: صور الوجبات المرفوعة المخزنة في سحابة خارجية.
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },

  // async headers():
  // دالة ترجع قائمة من "ترويسات" (headers) تُرسل مع كل استجابة.
  // الترويسة = معلومات إضافية ترسلها الصفحة للمتصفح.
  //
  // ترتيب التنفيذ:
  // عند كل طلب → يفحص Next.js القواعد → يضيف هذه الترويسات → المتصفح يستقبلها.
  async headers() {
    return [
      {
        // source: '/:path*' يعني: تنطبق على كل المسارات في الموقع.
        source: '/:path*',
        headers: [
          // تمنع عرض الموقع داخل iframe في موقع آخر (أمان).
          { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
          // تخبر المتصفح: لا تحاول "تخمين" نوع الملف (أمان ضد هجمات MIME).
          { key: 'X-Content-Type-Options', value: 'nosniff' },
          // تحدّد ما الذي يُرسل مع الروابط (خصوصية).
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
          // تسمح فقط للموقع نفسه باستخدام الكاميرا والإشعارات والميكروفون.
          {
            key: 'Permissions-Policy',
            value: 'camera=(self), notifications=(self), microphone=(self)',
          },
        ],
      },
    ];
  },
};

// export default:
// نُصدّر هذا الكائن ليكون الإعداد الافتراضي الذي يستخدمه Next.js.
// (export = نجعل هذا الملف قادرًا على إعطاء قيمة لملفات أخرى)
export default nextConfig;

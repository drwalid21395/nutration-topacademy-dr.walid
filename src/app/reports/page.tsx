/*
==================================================
شرح الملف للمبتدئ
==================================================

اسم الملف:
src/app/reports/page.tsx

وظيفة الملف:
صفحة "التقارير" (المسار /reports) — صفحة رقيقة تفحص
تسجيل الدخول ثم تعرض مكوّن Reports الذي يعرض تقارير
المستخدم (التزام، وزن، تغذية...) ويمكّنه من تصديرها.

لماذا نحتاجه؟
التقارير توضح تقدم السباح؛ والصفحة هنا غلاف حماية وعرض
للمكوّن الفعلي (الذي يعمل في المتصفح ويجلب بياناته).

نوعها: Server Component (بدون 'use client').
الفحص في الخادم قبل إرسال الصفحة.

متى يعمل؟
عند فتح /reports بعد تسجيل الدخول.

من يستدعي هذا الملف؟
Next.js يعرضه تلقائيًا؛ يصل إليه المستخدم من زر
"التقارير" في لوحة التحكم والقائمة الجانبية.

الملفات التي يتعامل معها:
- getCurrentUser من lib/auth.
- AppShell من components/layout/app-shell.
- Reports من components/reports/reports.

ترتيب العمل:
1. فحص تسجيل الدخول (لو زائر → صفحة الدخول).
2. عرض مكوّن التقارير داخل الإطار العام.
==================================================
*/

// ========================================
// 1. الاستيرادات
// ========================================

import { redirect } from 'next/navigation'; // redirect: نقل لصفحة أخرى — من مكتبة next/navigation.
import { getCurrentUser } from '@/lib/auth'; // دالة محلية: المستخدم المسجل حاليًا.
import { AppShell } from '@/components/layout/app-shell'; // الإطار العام — ملف محلي.
import { Reports } from '@/components/reports/reports'; // مكوّن عرض التقارير — ملف محلي.

// ========================================
// 2. بيانات التعريف
// ========================================

// metadata: عنوان الصفحة في تبويب المتصفح.
export const metadata = { title: 'التقارير' };

// ========================================
// 3. الصفحة الرئيسية (تعمل في الخادم)
// ========================================

// ReportsPage: الدالة الرئيسية للصفحة (تعمل في الخادم).
export default async function ReportsPage() {
  // الخطوة 1: من هو المستخدم؟ لو زائر → صفحة الدخول.
  const user = await getCurrentUser();
  if (!user) redirect('/login');

  // الخطوة 2: عرض مكوّن التقارير (هو من يجلب البيانات ويعرضها).
  return (
    <AppShell user={user}>
      <Reports />
    </AppShell>
  );
}

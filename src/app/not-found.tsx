/*
==================================================
شرح الملف للمبتدئ
==================================================

اسم الملف:
src/app/not-found.tsx

وظيفة الملف:
صفحة 404 — تظهر تلقائيًا عند زيارة رابط غير موجود
في المشروع. تصميم مائي مرعب لطيف "غاصت الصفحة في المسبح"
مع زرين للعودة.

لماذا نحتاجه؟
بدل رسالة خطأ مملة، يرى الزائر صفحة جذابة تعيده
بسرعة إلى الرئيسية أو لوحة التحكم.

نوعها: Server Component (بدون 'use client') — صفحة
ثابتة بلا بيانات.

متى يعمل؟
عند محاولة فتح أي مسار غير معرَّف (مثل /xyz)،
أو عند استدعاء notFound() من أي صفحة.

من يستدعي هذا الملف؟
Next.js يعرضه تلقائيًا في أي حالة "صفحة غير موجودة".
(أيضًا بعض الصفحات تستدعي دالة notFound من next/navigation).

الملفات التي يتعامل معها:
- Link من next/link فقط (روابط العودة).
- أيقونة Waves من lucide-react.

ترتيب العمل:
1. عرض أيقونة الموج والعنوان 404.
2. رسالة توضيحية.
3. زر العودة للرئيسية وزر لوحة التحكم.
==================================================
*/

// ========================================
// 1. الاستيرادات
// ========================================

import Link from 'next/link'; // رابط داخلي — من مكتبة next/link.
import { Waves } from 'lucide-react'; // أيقونة الموج من مكتبة lucide-react الخارجية.

// ========================================
// 2. بيانات التعريف
// ========================================

// metadata: عنوان الصفحة في تبويب المتصفح.
export const metadata = { title: 'الصفحة غير موجودة' };

// ========================================
// 3. الصفحة الرئيسية (واجهة 404)
// ========================================

// NotFound: الدالة الرئيسية — تعرض واجهة خطأ 404.
// ليس لها أي بيانات أو أحداث؛ واجهة ثابتة.
export default function NotFound() {
  return (
    // خلفية داكنة مائية تمتد لكامل الشاشة (min-h-screen).
    <div className="flex min-h-screen flex-col items-center justify-center bg-ocean-950 px-4 text-center text-white">
      {/* أيقونة الموج */}
      <Waves className="h-16 w-16 text-ocean-400" />
      <h1 className="mt-6 text-5xl font-black">404</h1>
      <p className="mt-2 text-lg font-bold">هذه الصفحة غاصت في المسبح ولم نجدها</p>
      <p className="mt-1 max-w-md text-sm text-slate-400">ربما الرابط غير صحيح أو الصفحة أُزيلت. لنعد بك إلى اليابسة الآمنة.</p>
      <div className="mt-8 flex gap-3">
        {/* زر العودة للرئيسية */}
        <Link href="/" className="btn-gold">العودة للرئيسية</Link>
        {/* زر لوحة التحكم (تنسيق شفاف ليلي) */}
        <Link href="/dashboard" className="btn-secondary !border-white/20 !bg-white/10 !text-white hover:!bg-white/20">لوحة التحكم</Link>
      </div>
    </div>
  );
}

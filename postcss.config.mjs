/*
==================================================
شرح الملف للمبتدئ
==================================================

اسم الملف:
postcss.config.mjs

وظيفة الملف:
إعدادات PostCSS — أداة "تجهز" ملفات CSS قبل وصولها للمتصفح.

لماذا نحتاجه؟
CSS الذي نكتبه (مثل bg-ocean-600) ليس CSS عاديًا يفهمه المتصفح
مباشرة. هذه الأداة تحوله:
1) tailwindcss: يقرأ ملفات المشروع ويولد CSS حقيقي من أسماء الفئات.
2) autoprefixer: يضيف البادئات المتوافقة مع المتصفحات تلقائيًا
   (مثل -webkit- للأجهزة القديمة).

متى يعمل؟
تلقائيًا مع كل بناء (dev أو build) قبل إرسال CSS للمتصفح.

ملاحظة تعليمية:
هذا الملف قصير جدًا لأن PostCSS مشروع "إعدادات فقط".
لو حذفته لن يعمل Tailwind إطلاقًا.
==================================================
*/

/** @type {import('postcss-load-config').Config} */
// JSDoc: تعليق يوضح لمحرر الأكواد نوع الكائن التالي — لا يؤثر على التشغيل.
const config = {
  plugins: {
    // tailwindcss: فعّل معالجة Tailwind داخل CSS.
    tailwindcss: {},
    // autoprefixer: أضف بادئات المتصفحات تلقائيًا.
    autoprefixer: {},
  },
};

// export default: نُصدّر الإعداد.
export default config;
